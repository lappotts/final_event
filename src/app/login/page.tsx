import React from "react";
import { LoginForm } from "./LoginForm";

export default function LoginPage() {
  return (
    <div className="p-10 flex items-center justify-center align middle min-h-screen">
      <LoginForm />
    </div>
  );
}
